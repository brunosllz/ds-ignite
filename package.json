{
  "private": true,
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint --parallel"
  },
  "devDependencies": {
    "turbo": "^1.6.3"
  }
}